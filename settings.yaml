# ───────────────────────────────────────
#  🎾 테니스 알림 설정표
#  이 표를 고치면 다음 점검(최대 5분)부터 반영됩니다.
#  · 받기: true(알림 받기) / false(끄기)
#  · 시간: 시작 시각 숫자만 — 20 = 저녁 8시
#  · 요일별로 적거나, 매일 같으면 '매일'로 한 줄
# ───────────────────────────────────────

강남:                      # 포이·세곡 (1시간 단위)
  받기: true
  매일: [19, 20, 21]       # 매일 저녁 7·8·9시

송파:
  받기: true
  토: [8, 10]              # 토요일 오전 8·10시

잠실:                      # 2시간 단위 시설(…16·18·20시)
  받기: true               # ← 잠깐 끄려면 false 로만 바꾸면 끝
  토: [8, 10, 18, 20]
  일: [18, 20]
  월: [20]
  화: [20]
  수: [20]
