# 비밀 토큰이 든 실제 환경파일 — 절대 git에 올리지 않음
.env
# 직전 빈자리 기록·런타임 캐시(로컬 실행용) — 클라우드에선 GitHub Actions 캐시 사용
state.json
status.json
failures.json
read_fail.json
olympic_state.json
# 파이썬 캐시
__pycache__/
*.pyc
.pytest_cache/
# 사이트 분석용 임시 스크립트(조사용, 배포와 무관)
_scratch/
# 설정표 직전 정상 백업(런타임 자동 생성)
settings.last_good.yaml
